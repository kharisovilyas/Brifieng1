<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Brifieng" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="bottom_save" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/black</item>
        <item name="android:backgroundTint">@color/accent_yellow</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:elevation">0dp</item>
    </style>

    <style name="DialogTheme" parent="Theme.MaterialComponents.DialogWhenLarge">
        <item name="windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@style/Animation.AppCompat.Dialog</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="FabButtonStyle" parent="Widget.MaterialComponents.FloatingActionButton">
        <!-- Цвет фона кнопки FAB -->
        <item name="backgroundTint">@color/blue_transparent</item>
        <!-- Цвет иконки на кнопке FAB -->
        <item name="android:tint">@color/black</item>
    </style>

</resources>